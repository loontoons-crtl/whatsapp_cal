<?xml version="1.0" encoding="utf-8"?>
<!-- Allow plain HTTP so the phone can reach the server on your local network
     (LAN addresses like 192.168.x.x don't use HTTPS). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
